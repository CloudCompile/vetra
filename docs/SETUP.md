# Setup

1. Install dependencies:
   - `npm install`
2. Copy env values:
   - use `.env.example` as reference
3. Generate Prisma client:
   - `npm run prisma:generate`
4. Run dev server:
   - `npm run dev`
